.env
.venv/
venv/
__pycache__/
.ipynb_checkpoints/
*.pyc
.DS_Store
.claude/

# ETL runtime data — gitignored except for fixtures under etl/tests/fixtures/
data/

# Large developer-local Discogs samples (kept out of the repo to avoid
# clone-time cost; download / generate locally per spec 002-etl-scaleup
# Assumptions section). Only the small raw sample is tracked.
etl/tests/fixtures/releases_sample_big_raw.xml

# Python packaging artifacts
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml

# Agent component runtime state (V1 chart artifacts; bind-mounted into the container)
artifacts/

# Agent component build / venv layers
agent/.venv/
agent/dist/
agent/build/
agent/*.egg-info/
