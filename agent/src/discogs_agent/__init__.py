"""Discogs conversational analytics agent — V1.

Component B per Constitution Principle VI. Reads only the
published DuckDB produced by the ETL component; never imports
from `discogs_etl`.
"""

__version__ = "0.1.0"
